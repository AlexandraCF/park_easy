<div class="login-main-container">
  <h2>Log in</h2>
  <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="login-form form-inputs">
      <%= f.input :email,
                  required: false,
                  autofocus: true,
                  input_html: { autocomplete: "email" } %>
      <%= f.input :password,
                  required: false,
                  input_html: { autocomplete: "current-password" } %>
      <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
      <%- if devise_mapping.recoverable? && controller_name != 'passwords' && controller_name != 'registrations' %>
      <% end %>
    </div>

    <div class="form-actions">
      <%= f.button :submit, "Log in", class: "btn-login-loginpage" %>
    </div>
  <% end %>
  <%= link_to "Forgot your password?", new_password_path(resource_name), class: "link-forgot-password" %>

  <div class="login-bottom">
    <p>First time here? <strong><%= link_to "Sign up", new_registration_path(resource_name) %></strong>.</p>
  </div>
</div>
